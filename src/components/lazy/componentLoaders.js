/**
 * Minimal Component Loaders
 *
 * Temporary fix for Vite build issues
 */

export async function preloadCriticalComponents() {
  // No-op for now - lazy loading temporarily disabled
  console.log('[LAZY_COMPONENTS] Preloading disabled temporarily');
}